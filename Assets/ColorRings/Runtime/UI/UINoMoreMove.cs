using System.Collections;
using System.Collections.Generic;
using Puzzle.UI;
using UnityEngine;

public class UINoMoreMove : MonoBehaviour, IPopUpContent
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
